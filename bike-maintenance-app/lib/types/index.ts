// Central SSOT exports — import from here, never from database.types directly
export { type Vehicle } from './vehicle';
export { type FuelLog } from './fuel';
export { type ServiceLog } from './service';
export { type UserProfile } from './auth';
